# $OpenBSD%

PKGNAME ?=	ostV_${OST_PKGNAME}

DIST_SUBDIR =	${OST_HOME}
SUBST_VARS =	OST_HOME OST_VM_VER OST_INTERP

OST_HOME =	opensmalltalk
OST_BIN_DIR =	${PREFIX}/lib/${OST_HOME}
OST_VM_VER ?=	${DISTNAME}
OST_INTERP ?=	${OST_IN}
