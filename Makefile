

     SUBDIR =
     SUBDIR += other_image
     SUBDIR += other_sources
     SUBDIR += other_vm
     SUBDIR += squeak_image
     SUBDIR += squeak_sources
     SUBDIR += squeak_vm

.include <bsd.port.subdir.mk>
